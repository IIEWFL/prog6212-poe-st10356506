<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    @* import bootstrap sytlesheet https://getbootstrap.com/docs/5.3/getting-started/download/*@
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Claim Submission Form</title>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Claim Submission Form</h1>

        @if (ViewBag.Error != null) @* https://learn.microsoft.com/en-us/aspnet/core/mvc/models/validation?view=aspnetcore-8.0&form=MG0AV3 *@
        {
            <div class="alert alert-danger">
                @ViewBag.Error @* if the uploaded document doesnt meet the requirements, the error message will be displayed https://learn.microsoft.com/en-us/aspnet/core/mvc/views/overview?view=aspnetcore-8.0&form=MG0AV3#viewbag-viewdata-and-tempdata*@
            </div>
        }

        @*   form containing the  input fields for the lecturer to submit their claims *@
        @* https://www.w3schools.com/html/html_forms.asp *@
        @* https://www.w3schools.com/html/html_forms_attributes.asp *@
        <form asp-action="ClaimForm" method="post" enctype="multipart/form-data" id="claim-form">

            <div class="mb-3">
                <label for="lecturerName" class="form-label">Name:</label>
                <input type="text" class="form-control" id="lecturerName" name="lecturerName" placeholder="Lecturer Username" required />
            </div>

            <div class="mb-3">
                <label for="lecturerEmail" class="form-label">Email:</label>
                <input type="text" class="form-control" id="lecturerEmail" name="lecturerEmail" placeholder="Lecturer Email" required />
            </div>

            <div class="mb-3">
                <label for="hoursWorked" class="form-label">Hours Worked:</label>
                <input type="number" id="hoursWorked" name="hoursWorked" class="form-control" min="1" required>
                <div class="invalid-feedback">
                    Please enter a valid number of hours worked. @* error handling here for invalid response *@
                </div>
            </div>

            <div class="mb-3">
                <label for="hourlyRate" class="form-label">Hourly Rate:</label>
                <input type="number" id="hourlyRate" name="hourlyRate" class="form-control" min="0" step="1" required>
                <div class="invalid-feedback">
                    Please enter a valid hourly rate. @* error handling here for invalid response *@
                </div>
            </div>

            <div class ="mb-3">
                <label for="calculatedTotal" class=" form-label"> Total claim amount: </label>
                <input type ="text" id="calculatedTotal" class="form-control" readonly /> 
            </div>

            <div class="mb-3">
                <label for="additionalNotes" class="form-label">Additional Notes:</label>
                <textarea id="additionalNotes" name="additionalNotes" class="form-control" rows="4"></textarea>
            </div>

            <div class="mb-3">
                <p>Optionally, provide supporting documents for your claim</p>
                <p>- Size limit is 5mb</p>
                <p>- Only .pdf, .docx, and .xlsx file types are allowed</p>
                <p>- Only 5 files are allowed</p>
                <label for="supportingDocuments" class="form-label">Upload Supporting Documents:</label>
                <input type="file" id="supportingDocuments" name="files" class="form-control" multiple>
            </div>

            <button type="submit" class="btn btn-primary">Submit Claim</button> @* button for submitting claim  *@
        </form>
    </div>

   @*  https://getbootstrap.com/docs/5.3/getting-started/download/ *@
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @*JQuery import https://www.codecademy.com/forum_questions/50c108eb79ff14bea60047aa#:~:text=There%20are%20FOUR%20ways%20that,%2Flibs%20%2Fjquery%2F1.7.*@
    <script src="https://code.jquery.com/jquery-latest.min.js"></script>

    @*auto calculation*@
    <script>
        $(document).ready(function () {
            //read from input fields
            $("#hoursWorked, #hourlyRate").on("input", function () {
                const hoursWorked = parseFloat($("#hoursWorked").val()) || 0;
                const hourlyRate = parseFloat($("#hourlyRate").val()) || 0;
                const calculatedTotal = hoursWorked * hourlyRate;

                //display the output in the calculatedTotal field
                $("#calculatedTotal").val(`R${calculatedTotal.toFixed(2)}`);  
            });
        });
    </script>

</body>
</html>

 